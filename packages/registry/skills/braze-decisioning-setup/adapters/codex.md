# Codex Adapter — Braze Decisioning Studio Setup

1. Collect: experiment name, goal, audience, variant names
2. Run `braze_decisioning_scaffold` to create the experiment structure
3. Optionally run `braze_generate_copy` for each variant
4. Run `braze_optimization_audit` for pre-launch validation
5. Present the complete scaffold for review before implementation
